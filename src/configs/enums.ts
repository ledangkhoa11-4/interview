export enum ESortType {
  NAME_ASC = "name-asc",
  NAME_DESC = "name-desc",
  RATING_DESC = "rating-desc",
  PRICE_ASC = "price-asc",
  PRICE_DESC = "price-desc",
}

export enum EToastStatus {
  Info,
  Error,
  Success,
  Warning,
}